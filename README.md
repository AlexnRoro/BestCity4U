# Best City for You - 全球城市匹配测评

找到最适合你长期居住和工作的城市

## 🌟 特性

- ✅ **纯静态部署**：GitHub Pages 直接访问，无需后端
- 🌍 **全球覆盖**：70+ 全球主要城市
- 🎯 **精准匹配**：8 维度个性化评估算法
- 📊 **可解释结果**：维度贡献度分析 + Top 5 推荐
- 🔒 **隐私保护**：所有计算在浏览器本地完成，不上传数据
- 🔄 **可复现**：相同答案 → 相同结果

## 📦 技术栈

- Vite + React + TypeScript
- Hash Router（避免 GitHub Pages 刷新 404）
- 纯前端匹配算法
- 静态 JSON 数据包

## 🚀 本地运行

```bash
# 安装依赖
npm install

# 生成城市数据
python tools/build_city_pack.py

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

## 📁 项目结构

```
Best City for You/
├── public/
│   └── data/
│       ├── questions.v1.json      # 题库数据
│       └── cities.lite.v1.json    # 城市数据包
├── src/
│   ├── pages/
│   │   ├── Home.tsx               # 首页
│   │   ├── Quiz.tsx               # 答题页
│   │   └── Results.tsx            # 结果页
│   ├── utils/
│   │   └── matching.ts            # 匹配算法
│   ├── types.ts                   # 类型定义
│   └── main.tsx                   # 入口文件
├── tools/
│   └── build_city_pack.py         # 城市数据生成脚本
└── vite.config.ts                 # Vite 配置
```

## 🎯 8 维度评估

1. **Climate** - 气候舒适度
2. **Cost** - 生活成本
3. **Safety** - 安全与稳定
4. **Mobility** - 交通便利
5. **Career** - 职业机会
6. **Culture** - 文化生活
7. **Nature** - 自然环境
8. **International** - 国际化程度

## 🔧 匹配算法

### 用户画像计算
- 从 30 道题目中提取 8 维度偏好分数（0-1）
- 计算每个维度的重视程度权重（0-1）
- 支持反向计分和多维度映射

### 城市匹配排序
1. **相似度计算**：`similarity = 1 - |user_score - city_score|`
2. **加权得分**：`base_score = Σ(weight × similarity) / Σ(weight)`
3. **惩罚机制**：高权重维度 + 低城市分数 → 额外惩罚
4. **最终分数**：`final_score = clamp(base_score - penalty, 0, 1) × 100`

### 可解释输出
- 维度贡献度（Contribution）
- Top 3 匹配优势
- Top 2 风险提示
- 数据置信度

## 📊 数据更新

更新城市数据：

1. 编辑 `tools/build_city_pack.py`
2. 运行 `python tools/build_city_pack.py`
3. 新数据自动生成到 `public/data/cities.lite.v1.json`

## 🌐 GitHub Pages 部署

项目已配置 GitHub Actions 自动部署：

1. Push 代码到 GitHub
2. Actions 自动构建
3. 部署到 `gh-pages` 分支
4. 访问 `https://[username].github.io/Best-City-for-You/`

手动部署：

```bash
npm run build
# 将 dist/ 目录内容推送到 gh-pages 分支
```

## 📝 License

MIT

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
